const numeros = [8, 11, 4, 1];
let resultado = 0;
for (num of numeros) {
    let i = 0 < numeros.length; 
    if (num - i > resultado ) {
        resultado = num - i
    }
 i++

}

    console.log(resultado)